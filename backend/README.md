# backend

后端开发区，目前预留给后续 Codex 协作。

建议第一阶段目标：

1. 从 `data/source/菜品菜单.xlsx` 和 `data/source/motto.txt` 生成可供前端读取的 JSON。
2. 提供今日菜单 API，保持“同一天菜单固定，主动重抽才刷新”。
3. 保留本地静态 PWA 发布路径，后端不要阻塞 GitHub Pages 版本。

建议接口见 `docs/CODEX_HANDOFF.md`。

